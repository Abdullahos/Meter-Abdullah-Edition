# Meter-Abdullah-Edition
meter on rasp 4 with SQLite database installed, built and ready to use

# Requirements:
install sqlite
```
sudo apt-get install libssl-dev
sudo apt-get install libsqlite3-dev
```
